spring:
  application:
    name: springcloud-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: gray
        group: DEFAULT_GROUP
    config:
      discovery:
        enabled: true
  profiles:
    active: dev